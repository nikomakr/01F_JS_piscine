// Travel Promotion Checker

// Check if user qualifies for the promotion
if (user.age >= 18 && user.age <= 25 && user.activeMembership === true) {
  console.log("You can benefit from our special promotion");
}
